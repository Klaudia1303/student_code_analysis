x= int(input('numero x: '))
y= int(input('numero y: '))
numstamp = 0

while numstamp<=10:
    if numstamp !=x and numstamp !=y:
        print(numstamp)
        numstamp = numstamp+1
    else:
        numstamp = numstamp+1
